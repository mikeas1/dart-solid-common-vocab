name: solid_common_vocab
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  rdf_vocab_gen: ^1.0.0
  build_runner: ^2.1.7

dependency_overrides:
  rdf_vocab_gen:
    path: /home/mike/projects/dart-rdf-vocab-gen
  rdf:
    path: /home/mike/projects/dart-rdf
